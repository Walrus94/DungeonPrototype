package org.dungeon.prototype.bot.state;

public enum ChatState {
    IDLE,
    PRE_GAME_MENU,
    GAME_MENU,
    AWAITING_NICKNAME,
    GENERATING_ITEMS,
    GENERATING_PLAYER,
    GENERATING_LEVEL,
    GAME,
    BATTLE,
}
